bind 0.0.0.0
port 6379
protected-mode no

save 900 1
save 300 10
save 60 10000

dir /data
dbfilename dump.rdb

appendonly yes
appendfsync everysec

maxmemory 512mb
maxmemory-policy allkeys-lru

logfile /data/redis.log
loglevel debug

cluster-enabled no
supervised no
